//any
let a: any;
let b: number;
let c: boolean;
let d: string;
let e: number[] = [1, 2, 3, 4];
let f: any[] = [1, 'a', true, [1, 2, 3, 4]];

enum Color {
    Red, Green, Blue
};

let backgroundColor = Color.Blue;